# docker-hello-world

For installing dependencies run:

```sh
make install
```

To run webserver, run:

```sh
make run
```

To run linting, run:

```sh
make lint
```

To run tests, run:

```sh
make test
```
